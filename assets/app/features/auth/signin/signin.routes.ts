import { Routes } from '@angular/router';
import { SigninContainerComponent } from './components';

export const signinRoutes: Routes = [
  {
    path: 'signin',
    component: SigninContainerComponent
  }
];